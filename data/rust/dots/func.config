# Functions
google() {
    if [ -z "$1" ]; then
        echo "No argument supplied"
        return
    fi
    echo "Searching for $@"
    search_string="$@"
    open "https://www.google.com/search?q=$search_string"
}